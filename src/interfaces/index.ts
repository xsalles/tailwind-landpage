export interface ButtonProps {
    children: string;
    width: string;
    bgColor: string;
    fontColor: string;
}

export interface HeadingProps {
    children: string;
    fontColor: string;
    bgColor: string;
}